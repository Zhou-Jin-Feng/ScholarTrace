# SP-02 新问题与专项假设

验证状态：通过（含限制）\
日期：2026-09-22\
范围：阶段二新问题集、原文参考要点、专项假设、质量判定、资源约束和停止条件。

## 1. 冻结结果

- 新问题：8 个。
- Claim：42 条。
- Evidence：逐题合计 37 条；问题之间保留各自 Evidence 身份。
- 每题预先标注关键要点：3 个，共 24 个。
- 模型调用：0 次。
- Provider API 调用：0 次。
- 题集指纹：`a309a94ddbad8669166289c04d21821c2e07112a0c573871d8b64c300ce03ab0`。

公开清单：`evaluation/seeds/sp_02_new_question_manifest.json`\
冻结结果：`evaluation/reports/sp_02_question_freeze.json`\
私有输入：`agent/verification-ablation/SP-02/frozen_inputs.json`\
私有参考：`agent/verification-ablation/SP-02/human_reference.json`

## 2. 专项假设

强语义核验的价值可能集中在条件或结论冲突、跨论文外推和证据不完整的 Claim；普通低风险 Claim 可以使用单次综合基线，但必须显式保留 `unverified` 状态并确保可审计。

该表述是待验证假设，不是阶段一已证明的结论。阶段一结果为 `INCONCLUSIVE_CEILING`，因此本阶段没有把核验收益预设为成立。

## 3. 质量判定与回退

- 主指标：关键要点加权覆盖率。
- 允许回退：相对于完整核验方案，关键要点覆盖下降不超过 5 个百分点。
- 错误约束：新题集合计无支持事实论断不得比完整核验方案增加超过 1 个。
- 状态约束：被跳过的 Claim 必须保持 `unverified`，并可回溯到题目、输入和选择规则。
- 任何约束失败时，保留完整核验方案作为正式路径，候选方案仅保留在实验路径。

## 4. 资源约束

- 新问题数：8 个。
- 专项方案最多一轮。
- 每个方案每题最多一个报告请求。
- 候选选择性核验的强核验上限：32 次；实际费用必须在外部运行前按当前 Provider 和输入规模重新计算。
- 未知 usage、预算超限或请求状态不可恢复时停止派发，不自动重发。

## 5. 题集组成

题集同时包含普通事实问题、跨论文比较问题、证据不完整问题和条件/结论冲突问题。题目使用独立 `sp02-new-*` 身份，虽然由既有已验证 Evidence 包派生，但不加入历史 SA-02 formal 结果，也不用于冻结前的规则调整。

## 6. 下一阶段

SP-03 已完成选择性核验候选规则、运行器接缝、选择计划和离线回归。下一步为 SP-04：在固定题集上比较简单基线、原完整方案和改进方案，并审计全部被跳过的 Claim。

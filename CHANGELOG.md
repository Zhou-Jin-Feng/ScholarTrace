# 版本历史

本文件记录 ScholarTrace 的对外版本。`0.x` 为开发期内部编号；自 `1.0.0` 起为正式发布版本，编号遵循 `主版本.次版本.修订号` 语义。

## 1.0.1

- 修复依赖就绪探活：付费 Provider 的元数据探活现在携带模型凭据（此前对真实 Provider 会返回 401，面板永远显示不可用）。
- DocuMind 探活改用独立的更宽超时，避免冷启动的就绪检查被过短的超时误判。
- 新增学术检索源的有界元数据探活（arXiv / OpenAlex / Crossref / Semantic Scholar），已配置的检索源可被真实判为就绪。
- 工作台在打开时触发一次探活并按 2 分钟刷新，就绪面板不再在缓存过期后回落为"未探测"。

## 1.0.0 — 首个正式发布

**研究内核**

- 研究数据契约与评测基线；多源论文检索、身份归一化与去重合并；
- DocuMind 全文检索接入与可追溯证据生成；Paper / Claim / Evidence / Verification 结构化契约；
- 可恢复的多智能体研究编排；引用网络、独立证据核验与受限补查。

**研究工作台**

- 任务创建与两道审批（计划审批、执行审批）；预算时间线；有界队列、取消与优雅停机；
- SSE 实时事件、刷新续传与具名重连；四种报告导出（Markdown / HTML / PDF / JSON）；
- 证据与核验阅读；受限 ScholarGraph 集成（评测未显示增益，默认关闭）。

**评测与结论（如实保留负面结果）**

- 图谱增强（GraphHint）离线与查漏评测：未观察到增益，维持禁用；
- 真实漏检采集与人工审核账本；前瞻 Gate A 结论为 `INCONCLUSIVE`；上游排序修复的冻结记录。

**可靠性与真实性**

- 确定性 Demo 与真实链路严格分离，Demo 结果不作为研究质量证据；
- 本地一致性快照备份与只写新目录的恢复；版本化公开 arXiv 全文获取与 DocuMind 归属校验；
- 生产装配入口：双门禁授权、统一预算与计量、外部调用 fail-closed。

**工程化**

- 质量工作流（后端 / 前端 / 浏览器）、确定性源码打包、依赖锁定与版本一致性检查、仓库卫生与凭据扫描。

**边界**

- 默认演示不调用真实模型或付费路由；真实在线全链路（真实搜索、全文获取、在线 DocuMind、Provider 账单）需按运行手册配置并单独验收；
- 当前部署定位为本地单用户工作台，不承诺公网多用户、跨进程队列或跨服务灾备。

## 0.1.0 – 0.5.0（开发期内部编号，未单独发布）

| 内部编号 | 主要内容 |
|---|---|
| `0.1.0` | 研究数据契约与评测基线 |
| `0.2.0` | 多源论文检索与归一化；DocuMind 接入与可追溯证据生成 |
| `0.3.0` | 可恢复的多智能体研究编排 |
| `0.4.0` | 引用网络与证据核验流程 |
| `0.5.0` | 研究工作台与受限图谱集成；评测题集跨平台修复；图谱查漏评测结论；真实漏检采集与审核账本；前瞻采集与候选收益门禁；任务可靠性与本地备份恢复 |

> 0.x 期间的提交保留其当时的内部版本号与实验记录；自 1.0.0 起使用统一的正式版本线。
